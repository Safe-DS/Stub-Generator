package safeds.exceptions

from safeds.exceptions import Bound

/**
 * An open Bound, i.e. the value on the border does not belong to the range.
 *
 * @param value The value of the OpenBound.
 */
class OpenBound(
    value: Float
) sub Bound
